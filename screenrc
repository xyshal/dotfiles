# Global options
autodetach on
shell zsh
defscrollback 50000
startup_message off

# Keybindings
bind + resize +10
bind - resize -10
bind = resize =
bindkey -k k1 focus
bindkey -k k2 prev
bindkey -k k3 next

# Initial windows
screen -t vim 0
screen -t zsh 1
select 0

